### Please config DB env first. (postgresql)

see `.env.example`, copy it to `.env` and change `DATABASE_URL`

#### Then run

```
npm install
npm run dev
```

#### Postman Collection is located at `neversitup.postman_collection.json`
